"""Sales admin website API.

Each endpoint is its own module (one view class per file) so the project
grows predictable, discoverable files. Routes are declared in
:mod:`api.sales_admin.urls` and mounted under ``/api/``.
"""
